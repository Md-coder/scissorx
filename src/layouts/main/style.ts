import { styled } from '@mui/material/styles';

export const Logo = styled('a')({
  outline: 'none',
  alignItems: 'center',
  display: 'inline-flex ',
  width: '168px',
  height: '40px',
  position: 'relative',
  verticalAlign: 'middle',
  zIndex: 1,
  textDecoration: 'none',
});
